#!/bin/bash

if pgrep -x "picom" > /dev/null
then
	killall picom &
	sleep 1; picom -b --experimental-backends --config ~/.config/picom.conf; notify-send "`pgrep picom`" &
else
	picom -b --experimental-backends --config ~/.config/picom.conf; notify-send "`pgrep picom`" &
fi
